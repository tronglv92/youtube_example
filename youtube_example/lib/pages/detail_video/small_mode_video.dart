import 'package:flutter/material.dart';
class SmallModeVideo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
      child: Column(
        children: [
          Container(
              height: 200,
              color: Colors.red,
              child:Row(
                children: [

                ],
              )
          ),

        ],
      ),
    );
  }
}
